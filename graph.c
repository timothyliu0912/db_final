#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "graph.h"

int sent_insert(graph *db,char *sent){
    char *ptr = sent;
    int w_cnt = 0;
    char tmp[100];
    memset(tmp,'\0',sizeof(char)*100);
    while (ptr){
        scanf(ptr,tmp);
    }
    
}